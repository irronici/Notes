# Review

### 第一章

+ 数据
+ 数据库
  + 特点：结构化、集中存储、数据共享
+ 数据库管理系统DBMS
  + 特点
    + 持久性数据管理
    + 对大量数据进行有效存取
    + 多用户存取同一数据
+ **数据库系统DBS**
  + DB+DBMS+DBA+软件平台+硬件平台**（考过）**

+ 数据库基本特点
  + 集成 高功效低冗余...（4个）
+ 三级模式两种映射的概念

### 第二章

+ 数据模型
  + 组成：数据结构+数据操作+数据约束
+ 数据模型转换为数据库的三种数据模型
  + 三种数据模型与三级模式之间的关系
    + 外模式（提供视图）
+ 四个世界
  + 概念世界
    + 几种概念模型
      + E-R、EE-R（继承+弱实体）、（面向对象模型、谓词模型，了解即可）
+ 关系
  + 行和列组成的二维表格
  + 约束
    + 同一表中属性名不同
    + 属性排放次序无关
    + 表中元组不同
    + 元组次序不同
    + 每一分量是不可分割的基本数据项

### 第三章

+ **占分多**
+ 关系代数

  + 关系上五种基本运算与关系代数运算的对应关系
  + 关系的表示
    + 元组的集合
  + 关系操纵的表示
    + 几种运算
+ SQL

  + 基表创建 Create
  + 基表修改 Alter
  + 基表删除 Drop

  + 映像语句
 	+ 映像语句
      + SELECT
    + FROM 范围
    + WHERE 条件
    + GROUP BY 分组（只能select出现在group by中的属性/聚集函数）
    + HAVING 分组查询
    + ORDER BY 
+ AS、LIKE、自连接（在where中进行）、（NOT）IN、ANY|ALL、（NOT）EXIST 
+ UNION INTERSECT EXCEPT 
  + 会自动去冗余
  + 要不去除冗余则在命令后加上"ALL"
+ 删除 DELETE
  + 删除符合条件的元组
+ 插入 insert into values / insert into subquery
+ 修改 update
  + 修改符合条件的元组
+ 视图
  + 视图定义 Create view

### 第四章

+ 数据库安全

+ 授权语句 GRANT <操作权限列表> ON <操作对象> TO <用户名列表> [WITH GRANT OPTION]

+ 回收语句 REVOKE <操作权限列表> ON <操作对象> FROM <用户名列表> RESTRICT|CASCADE

+ 数据库的完整性
  + 指DB中数据的正确性和一致性
    + 正确性：数据的有效性、有意义
    + 一致性
  + 提供了三类完整性约束
    + 实体完整性规则（主关键字的属性非空）
    + 参照完整性
    + 用户定义的完整性规则（触发器不考）

### 第五章

+ 事物
  + 由某用户所执行的不能被打断的对数据库的操作序列
+ **ACID**
  + 原子性 Atomicity
  + 一致性 Consistency
  + 隔离性 Isolation
  + 持久性 Durability
+ 并发执行
  + 调度
  + 串行调度
  + 冲突
  + 冲突等价
    + 通过一系列相邻操作的菲冲突交换将其转换为另一个调度
  + 冲突可串行化
    + 是串行化的充分条件，不是必要条件
+ 封锁
  + 封锁协议
  + 两阶段封锁协议
  + 多粒度封锁协议（了解一下就行）
    + 意向锁
  + 死锁/活锁（了解即可）
+ 日志
  + undo日志
    + V记录的是旧值
  + redo日志
    + V记录的是更新后的值
  + undo/redo日志
    + v记录旧值，w记录新值

### 第六章

+ 游标管理
+ 动态SQL

### 第七章

+ 记录在磁盘中间的分配方式
  + 单块单记录、单块多记录
    + 有无用部分、不能跨块
  + 多块单记录 有无用部分
  + 多块多记录 不存在空间浪费
+ 索引
  + 稠密索引
    + 数据文件中的每条记录在索引文件中都存在对应的索引项
  + 系数索引
    + 若数据文件为顺序文件，则没个磁盘块只记录一个记录
+ B/B+树索引文件
  + 特点
+ 散列技术

### 第八章

+ 函数依赖
  + 部分/完全FD，平凡/非平凡FD，直接/传递FD
+ 多值依赖（不考）

+ 模式分解
  + 无损连接性的充要条件
  + 无损分解到三范式的算法

### 第九章

+ 见笔记

### 第十章

+ 数据库管理

